// FILE IS GENERATED! DON'T EDIT! Generated by apigen.js Sat Nov 04 2017 19:56:23 GMT+0200 (EET)

const
    logger = require('../logger'),
	got = require('got');

async function ping(host) {
    var res = (await got(host + '/api/v1/ping',{json:true})).body;
    return res;
};

async function connect(host) {
    var res = (await got(host + '/api/v1/connect',{json:true})).body;
    return res;
};

async function disconnect(host) {
    var res = (await got(host + '/api/v1/disconnect',{json:true})).body;
    return res;
};

async function nodeRate(host) {
    var res = (await got(host + '/api/v1/nodeRate',{json:true})).body;
    return res;
};

async function nodeState(host) {
    var res = (await got(host + '/api/v1/nodeState',{json:true})).body;
    return res;
};

async function poolNotifications(host) {
    var res = (await got(host + '/api/v1/poolNotifications',{json:true})).body;
    return res;
};

module.exports = {
    ping: ping,
    connect: connect,
    disconnect: disconnect,
    nodeRate: nodeRate,
    nodeState: nodeState,
    poolNotifications: poolNotifications
}
